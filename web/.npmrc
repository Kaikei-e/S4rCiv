engine-strict=true

# Do not run an implicit install before `pnpm run/exec`; the test image and CI
# run an explicit `pnpm install` first. Avoids the no-op-install ignored-build gate.
verify-deps-before-run=false
